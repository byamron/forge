# Forge runtime state (per-user, not project config)
.claude/forge/

# Per-user local config from /remember
.claude/CLAUDE.local.md

# Scoring evaluation labeled data (contains real user messages)
tests/scoring_eval/labeled/*.json

# Ephemeral evaluation output
eval_report.txt
eval_result.json

# Python
__pycache__/
*.pyc

# macOS
.DS_Store
